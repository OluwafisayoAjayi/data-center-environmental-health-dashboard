# Dashboard data files

`demo_dashboard_county.csv` is only for local preview before the GitHub Action downloads public data.

When the update pipeline runs, it creates:

- `dashboard_county_latest.csv`
- `dashboard_metadata.json`
- `data_dictionary.csv`

Do not cite the demo data as empirical results.
